/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { ON_CHAIN_LOTTERY_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INIT_VAULT_DISCRIMINATOR = new Uint8Array([
  77, 79, 85, 150, 33, 217, 52, 106,
]);

export function getInitVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_VAULT_DISCRIMINATOR);
}

export type InitVaultInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountVaultAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultAuthority extends string
        ? WritableSignerAccount<TAccountVaultAuthority> &
            AccountSignerMeta<TAccountVaultAuthority>
        : TAccountVaultAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  locked: boolean;
};

export type InitVaultInstructionDataArgs = { locked: boolean };

export function getInitVaultInstructionDataEncoder(): FixedSizeEncoder<InitVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['locked', getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_VAULT_DISCRIMINATOR })
  );
}

export function getInitVaultInstructionDataDecoder(): FixedSizeDecoder<InitVaultInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['locked', getBooleanDecoder()],
  ]);
}

export function getInitVaultInstructionDataCodec(): FixedSizeCodec<
  InitVaultInstructionDataArgs,
  InitVaultInstructionData
> {
  return combineCodec(
    getInitVaultInstructionDataEncoder(),
    getInitVaultInstructionDataDecoder()
  );
}

export type InitVaultAsyncInput<
  TAccountVaultAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultAuthority: TransactionSigner<TAccountVaultAuthority>;
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  locked: InitVaultInstructionDataArgs['locked'];
};

export async function getInitVaultInstructionAsync<
  TAccountVaultAuthority extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
>(
  input: InitVaultAsyncInput<
    TAccountVaultAuthority,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitVaultInstruction<
    TProgramAddress,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ON_CHAIN_LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          expectAddress(accounts.vaultAuthority.value)
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitVaultInstructionDataEncoder().encode(
      args as InitVaultInstructionDataArgs
    ),
    programAddress,
  } as InitVaultInstruction<
    TProgramAddress,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type InitVaultInput<
  TAccountVaultAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultAuthority: TransactionSigner<TAccountVaultAuthority>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  locked: InitVaultInstructionDataArgs['locked'];
};

export function getInitVaultInstruction<
  TAccountVaultAuthority extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
>(
  input: InitVaultInput<
    TAccountVaultAuthority,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitVaultInstruction<
  TProgramAddress,
  TAccountVaultAuthority,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ON_CHAIN_LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitVaultInstructionDataEncoder().encode(
      args as InitVaultInstructionDataArgs
    ),
    programAddress,
  } as InitVaultInstruction<
    TProgramAddress,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedInitVaultInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultAuthority: TAccountMetas[0];
    vault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitVaultInstructionData;
};

export function parseInitVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultAuthority: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitVaultInstructionDataDecoder().decode(instruction.data),
  };
}
