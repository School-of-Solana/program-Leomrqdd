/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { ON_CHAIN_LOTTERY_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLAIM_IF_WINNER_DISCRIMINATOR = new Uint8Array([
  2, 233, 107, 218, 195, 20, 238, 65,
]);

export function getClaimIfWinnerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_IF_WINNER_DISCRIMINATOR
  );
}

export type ClaimIfWinnerInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountVaultAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountParticipant extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountVaultAuthority extends string
        ? ReadonlyAccount<TAccountVaultAuthority>
        : TAccountVaultAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountParticipant extends string
        ? WritableAccount<TAccountParticipant>
        : TAccountParticipant,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimIfWinnerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClaimIfWinnerInstructionDataArgs = {};

export function getClaimIfWinnerInstructionDataEncoder(): FixedSizeEncoder<ClaimIfWinnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_IF_WINNER_DISCRIMINATOR })
  );
}

export function getClaimIfWinnerInstructionDataDecoder(): FixedSizeDecoder<ClaimIfWinnerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimIfWinnerInstructionDataCodec(): FixedSizeCodec<
  ClaimIfWinnerInstructionDataArgs,
  ClaimIfWinnerInstructionData
> {
  return combineCodec(
    getClaimIfWinnerInstructionDataEncoder(),
    getClaimIfWinnerInstructionDataDecoder()
  );
}

export type ClaimIfWinnerAsyncInput<
  TAccountUser extends string = string,
  TAccountVaultAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountParticipant extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  vaultAuthority: Address<TAccountVaultAuthority>;
  vault?: Address<TAccountVault>;
  participant?: Address<TAccountParticipant>;
};

export async function getClaimIfWinnerInstructionAsync<
  TAccountUser extends string,
  TAccountVaultAuthority extends string,
  TAccountVault extends string,
  TAccountParticipant extends string,
  TProgramAddress extends Address = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
>(
  input: ClaimIfWinnerAsyncInput<
    TAccountUser,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountParticipant
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClaimIfWinnerInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountParticipant
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ON_CHAIN_LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    participant: { value: input.participant ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          expectAddress(accounts.vaultAuthority.value)
        ),
      ],
    });
  }
  if (!accounts.participant.value) {
    accounts.participant.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 97, 114, 116, 105, 99, 105, 112, 97, 110, 116])
        ),
        getAddressEncoder().encode(expectAddress(accounts.vault.value)),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.participant),
    ],
    data: getClaimIfWinnerInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimIfWinnerInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountParticipant
  >);
}

export type ClaimIfWinnerInput<
  TAccountUser extends string = string,
  TAccountVaultAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountParticipant extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  vaultAuthority: Address<TAccountVaultAuthority>;
  vault: Address<TAccountVault>;
  participant: Address<TAccountParticipant>;
};

export function getClaimIfWinnerInstruction<
  TAccountUser extends string,
  TAccountVaultAuthority extends string,
  TAccountVault extends string,
  TAccountParticipant extends string,
  TProgramAddress extends Address = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
>(
  input: ClaimIfWinnerInput<
    TAccountUser,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountParticipant
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimIfWinnerInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountVaultAuthority,
  TAccountVault,
  TAccountParticipant
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ON_CHAIN_LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    participant: { value: input.participant ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.participant),
    ],
    data: getClaimIfWinnerInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimIfWinnerInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountVaultAuthority,
    TAccountVault,
    TAccountParticipant
  >);
}

export type ParsedClaimIfWinnerInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    vaultAuthority: TAccountMetas[1];
    vault: TAccountMetas[2];
    participant: TAccountMetas[3];
  };
  data: ClaimIfWinnerInstructionData;
};

export function parseClaimIfWinnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimIfWinnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      vaultAuthority: getNextAccount(),
      vault: getNextAccount(),
      participant: getNextAccount(),
    },
    data: getClaimIfWinnerInstructionDataDecoder().decode(instruction.data),
  };
}
