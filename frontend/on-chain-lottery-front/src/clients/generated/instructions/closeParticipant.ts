/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { ON_CHAIN_LOTTERY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_PARTICIPANT_DISCRIMINATOR = new Uint8Array([
  192, 162, 92, 5, 148, 191, 207, 151,
]);

export function getCloseParticipantDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_PARTICIPANT_DISCRIMINATOR
  );
}

export type CloseParticipantInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountParticipant extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountParticipant extends string
        ? WritableAccount<TAccountParticipant>
        : TAccountParticipant,
      ...TRemainingAccounts,
    ]
  >;

export type CloseParticipantInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseParticipantInstructionDataArgs = {};

export function getCloseParticipantInstructionDataEncoder(): FixedSizeEncoder<CloseParticipantInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_PARTICIPANT_DISCRIMINATOR })
  );
}

export function getCloseParticipantInstructionDataDecoder(): FixedSizeDecoder<CloseParticipantInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseParticipantInstructionDataCodec(): FixedSizeCodec<
  CloseParticipantInstructionDataArgs,
  CloseParticipantInstructionData
> {
  return combineCodec(
    getCloseParticipantInstructionDataEncoder(),
    getCloseParticipantInstructionDataDecoder()
  );
}

export type CloseParticipantInput<
  TAccountUser extends string = string,
  TAccountParticipant extends string = string,
> = {
  /** The user who owns the participant account */
  user: TransactionSigner<TAccountUser>;
  /**
   * The participant account to close
   * The rent will be refunded to the user
   */
  participant: Address<TAccountParticipant>;
};

export function getCloseParticipantInstruction<
  TAccountUser extends string,
  TAccountParticipant extends string,
  TProgramAddress extends Address = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
>(
  input: CloseParticipantInput<TAccountUser, TAccountParticipant>,
  config?: { programAddress?: TProgramAddress }
): CloseParticipantInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountParticipant
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ON_CHAIN_LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    participant: { value: input.participant ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.participant),
    ],
    data: getCloseParticipantInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseParticipantInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountParticipant
  >);
}

export type ParsedCloseParticipantInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The user who owns the participant account */
    user: TAccountMetas[0];
    /**
     * The participant account to close
     * The rent will be refunded to the user
     */
    participant: TAccountMetas[1];
  };
  data: CloseParticipantInstructionData;
};

export function parseCloseParticipantInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseParticipantInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { user: getNextAccount(), participant: getNextAccount() },
    data: getCloseParticipantInstructionDataDecoder().decode(instruction.data),
  };
}
