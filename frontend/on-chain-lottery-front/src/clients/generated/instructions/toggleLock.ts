/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { ON_CHAIN_LOTTERY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const TOGGLE_LOCK_DISCRIMINATOR = new Uint8Array([
  188, 144, 112, 220, 80, 184, 226, 12,
]);

export function getToggleLockDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TOGGLE_LOCK_DISCRIMINATOR);
}

export type ToggleLockInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountVaultAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultAuthority extends string
        ? ReadonlySignerAccount<TAccountVaultAuthority> &
            AccountSignerMeta<TAccountVaultAuthority>
        : TAccountVaultAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type ToggleLockInstructionData = { discriminator: ReadonlyUint8Array };

export type ToggleLockInstructionDataArgs = {};

export function getToggleLockInstructionDataEncoder(): FixedSizeEncoder<ToggleLockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: TOGGLE_LOCK_DISCRIMINATOR })
  );
}

export function getToggleLockInstructionDataDecoder(): FixedSizeDecoder<ToggleLockInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getToggleLockInstructionDataCodec(): FixedSizeCodec<
  ToggleLockInstructionDataArgs,
  ToggleLockInstructionData
> {
  return combineCodec(
    getToggleLockInstructionDataEncoder(),
    getToggleLockInstructionDataDecoder()
  );
}

export type ToggleLockInput<
  TAccountVaultAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  vaultAuthority: TransactionSigner<TAccountVaultAuthority>;
  vault: Address<TAccountVault>;
};

export function getToggleLockInstruction<
  TAccountVaultAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
>(
  input: ToggleLockInput<TAccountVaultAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): ToggleLockInstruction<
  TProgramAddress,
  TAccountVaultAuthority,
  TAccountVault
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ON_CHAIN_LOTTERY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vault),
    ],
    data: getToggleLockInstructionDataEncoder().encode({}),
    programAddress,
  } as ToggleLockInstruction<
    TProgramAddress,
    TAccountVaultAuthority,
    TAccountVault
  >);
}

export type ParsedToggleLockInstruction<
  TProgram extends string = typeof ON_CHAIN_LOTTERY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultAuthority: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: ToggleLockInstructionData;
};

export function parseToggleLockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedToggleLockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vaultAuthority: getNextAccount(), vault: getNextAccount() },
    data: getToggleLockInstructionDataDecoder().decode(instruction.data),
  };
}
